#include<stdio.h>
int counting(int s[n])
{	
	int z,flag;
	for(flag=0;flag<n-1;flag)

	return (z);
}
int main()
{	
	int num, str[100],i,flag;
	str[num - 1] = '\0';
	counting(str,num);


	return 0;
}

